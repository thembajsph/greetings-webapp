language: node_js
node_js:
  - 12
  services:
  - postgresql
  directories:
- node_modules
  before_script:
    npm install
  - psql -c 'create database  greetings;' -U postgres
  script:
- npm run test